free a,b,c.
const alpha.
fun f/1.

let P = in(a,x); (in(b,y);out(c,c) | in(c,z);out(c,c)).
let R = in(c,x); (in(c,y); out(c,x) | in(c,z); in(c,zz)).

query sess_equiv(R,R).


/* Known issues to be addressed:
1. During the reflexivity check of in(a,x); (in(b,y);out(c,c) | in(c,z);out(c,c)), a Constraint_system_set.find fails. */
