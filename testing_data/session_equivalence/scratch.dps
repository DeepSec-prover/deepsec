free c.
/* fun h/2. */
/* reduc h(x,x) -> x. */

/* let P = in(c,x); in(c,y); out(c,h(x,y)). */
let P = !^4 out(c,c); in(c,x); out(c,c).
let Q = !^4 out(c,c); in(c,x); in(c,y).

query sess_equiv(P,Q).
