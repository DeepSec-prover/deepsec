free c.
const ok, error.
fun mac/2.


let A =
  /* in(c,yolo); // the attack is found without this dumb input */
  new n_t;
  out(c,n_t).

let B(k) =
  in(c,n_t);
  out(c,mac(n_t,k)).

let sys1 = !^2 A | new k; !^2 B(k).
/* let sys2 = !^2 A | new k; !^2 B(k). */


/* query sess_equiv(sys1,sys2). */
query trace_equiv(sys1,sys1).
